def find_index(arr, target, index=0):
    if index == len(arr):
        return -1
    if arr[index] == target:
        return index
    return find_index(arr, target, index + 1)

# Example Usage
arr = ["apple", "banana", "cherry"]
target = "banana"
print(find_index(arr, target))  # Output: 1
