def longest_word_length(words):
    if not words:
        return 0
    return max(len(words[0]), longest_word_length(words[1:]))

# Example
words_list = ["apple", "banana", "cherry", "date"]
print(longest_word_length(words_list))
